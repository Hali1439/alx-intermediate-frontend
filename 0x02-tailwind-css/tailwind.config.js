/** @type {import('tailwindcss').Config} */
module.exports = {
  content: ["./src/**/*.{html,js}"], // Scans all HTML & JS files in /src for classes
  theme: {
    extend: {}, // Where you'll add custom colors, spacing, fonts, etc.
  },
  plugins: [],
}
